#pragma once

namespace Params {
    // Game
    static constexpr int MAX_SCORE = 1;
    // Blocker
    constexpr int SPEED = 10;
    constexpr int BLOCKER_HEIGHT = 100;
    constexpr int BLOCKER_WIDTH = 20;
    // Ball
    constexpr float INITIAL_SPEED = 4.0f;
    constexpr float SPEED_INCREMENT = 1.0f;
    // Score
    constexpr int DISTANCE_Y = 30;
    constexpr float POSITION_DIVIDER = 4.f;
}